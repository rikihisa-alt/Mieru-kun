// =====================================================================
// テナント(店舗)識別。マルチテナント移行の橋渡し。
//
// 現状は認証・店舗選択が未実装のため、Phase1 では固定の「開発店舗ID」を
// 全データの store_id として使う。DBの暫定 anon RLS ポリシーもこのIDに限定して
// おり、開発中も店舗間分離を担保する(このIDの店舗しか触れない)。
//
// 認証/マルチテナント有効化時は、この定数参照をやめ、セッション(memberships)から
// 現在の店舗IDを解決する実装へ差し替える。呼び出し側(repository)は変更不要。
// SQL 側の開発店舗ID (phase1_stores_customers_visits.sql) と必ず一致させること。
// =====================================================================

export const DEV_STORE_ID = "00000000-0000-0000-0000-0000000000d5";

/** 現在の店舗ID。将来はセッション由来に差し替える。今は開発店舗固定。 */
export function getCurrentStoreId(): string {
  return DEV_STORE_ID;
}
